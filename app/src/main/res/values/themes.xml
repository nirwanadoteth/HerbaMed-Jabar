<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.HerbaMedJabar" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <!-- PERUBAHAN: Menambahkan baris ini untuk mengubah warna status bar -->
        <item name="android:statusBarColor">@color/status_bar_dark</item>
    </style>

    <style name="Theme.HerbaMedJabar" parent="Base.Theme.HerbaMedJabar">
        <!-- Status bar color. -->
        <!-- TAMBAHKAN DUA BARIS INI -->
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/bg_color</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_circle_welcome</item>
        <item name="postSplashScreenTheme">@style/Theme.HerbaMedJabar</item>
    </style>
</resources>